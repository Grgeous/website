Console.WriteLine("ILAN ELEMENTS ANG GUSTO MO?: ");
            int n = int.Parse(Console.ReadLine());

            string[] elements = new string[n];

            for(int i=0; i < n; i++)
           { 
                string  suffix = GetSuffix(i + 1);
                Console.Write("ELEMENTS:");
                elements[i] = Console.ReadLine();         
        }

          Console.Write("\nNEW ELEMENT: ");
              string newElement = Console.ReadLine();

              Console.Write("POSITION: ");
              int position=int.Parse(Console.ReadLine());

            for(int i=elements.Length-1; i >position; i--)
            {
                elements[i] =elements[1 - 1];
            }
                elements[position] = newElement;
            Console.WriteLine("\nRESULTS");
            foreach (string element in elements)
            {
             Console.WriteLine(elements);
            }
                Console.Write("THE INPUTTED POSITION IS SPACE ARRAY LENGTH:");
                int space = int.Parse(Console.ReadLine());           
            }

        static string GetSuffix(int num)
           {
                   if (num==1) return "ST";
                   if (num==1) return "ND";
                   if (num==1) return "RD";
                   return "TH";
        }
    }
}




2.
string[]position={"st","nd","rd","th"};
            Console.Write("ilan elements gusto mo");
            int ilan=int.Parse(Console.ReadLine());
            string[]elements= new string[ilan];

            for (int i = 0; i < ilan; i++)
			{
                Console.Write("{0}{1}elements:", i + 1, position[i]);
                elements[i]=Console.ReadLine();
			}
            Console.WriteLine("/nAll inputted  elements:/n");
            for (int i = 0; i < ilan;i++)
            {
                Console.WriteLine(elements[i]);
            }
            Console.Write("\nNEW ELEMENTS:");
            string newElement = Console.ReadLine();

            Console.Write("POSITION");
            int changePosition = int.Parse(Console.ReadLine()) - 1;
			{
			 
			}
        }
    }
}













